# Supabase Workspace

This folder will hold the database assets required by the assignment.

- `migrations/` stores SQL migrations for schema, constraints, RLS, triggers, and RPC functions.
- `seed/` stores repeatable seed data for demo flights, seats, and test accounts.

Do not commit real Supabase secrets. Use `.env.example` as the public contract for required environment variables.
