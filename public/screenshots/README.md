# Screenshots

Add your Lighthouse PWA audit screenshot here before submission:

- `lighthouse-pwa.png` — Chrome DevTools → Lighthouse → Progressive Web App (production URL, score ≥ 90)

Reference this file from the root README after capturing the audit against your deployed build.
